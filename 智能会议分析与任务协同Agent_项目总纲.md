# 面向企业会议场景的 AI Agent 智能分析与任务协同系统
## 项目总纲 / 产品思维与 Vibe Coding 上下文

> 文档用途：统一项目理解，供项目成员、AI Coding Agent（Claude Code / Codex 等）在后续开发过程中快速建立上下文。
>
> 本文档重点记录“为什么做、想做成什么、希望用户怎么使用、哪些东西值得做、哪些东西暂时不做”的思维与期望。
> **本文不是数据库设计、接口设计或代码实现文档。具体技术方案应在后续根据实际项目情况讨论、验证后确定。**

---

# 一、项目定位

项目名称暂定：

**面向企业会议场景的 AI Agent 智能分析与任务协同系统**

简称可称：

**智能会议分析与任务协同 Agent**

项目定位不是一个单纯的“AI 会议纪要工具”，也不是一个普通的企业 OA，更不是一个加入聊天框的传统管理系统。

希望形成的是一个：

**企业内部会议管理 + 会议内容智能分析 + 自动任务生成 + 任务协同 + 企业信息检索 + AI 工作助手**

的轻量级企业应用原型。

其核心思路是：

> **会议是组织信息产生的入口，AI 负责理解会议内容并把非结构化信息转化为结构化的决策、任务和知识；系统负责把这些结果提供给正确的人，并持续记录任务执行状态。**

最终形成：

**会议 → AI 理解 → 决策/事项/任务 → 人员关联 → 自动进入员工工作台 → 员工执行 → 状态更新 → 管理者掌握进度 → 历史信息可检索**

---

# 二、为什么不是单纯的 AI 会议总结

一个普通 AI 会议工具可能是：

1. 上传录音
2. 语音转文字
3. 大模型总结
4. 输出一篇会议纪要

这类产品容易变成“AI + 上传文件 + 总结”的套壳形态。

本项目希望进一步解决的问题是：

- 会议中究竟做出了哪些决定；
- 哪些内容只是讨论，哪些内容已经形成明确决定；
- 哪些人或部门被涉及；
- 哪些事情需要执行；
- 谁负责执行；
- 是否存在协作人员；
- 什么时间完成；
- 什么事情更紧急；
- 任务是否已经完成；
- 哪些任务正在逾期；
- 历史会议中有没有讨论过同一件事；
- 员工今天究竟需要做什么；
- 管理者能否快速了解整体执行情况。

因此，AI 的输出不能停留在一篇文本，而应尽可能成为系统中的“业务对象”。

---

# 三、核心产品思想

## 3.1 核心原则：AI 不只是回答，而是参与工作流程

项目希望 AI 参与：

- 会议内容理解
- 信息提取
- 人员/部门关联
- 决策识别
- 任务识别
- 任务优先级建议
- 历史会议检索
- 企业知识检索
- 面向员工的工作问答
- 必要的业务工具调用

但 AI 不应该无边界地直接控制数据库或系统。

整体思想：

> **Agent 负责理解和决策，业务后端负责权限、校验与执行，数据库负责保存事实。**

能够由确定性程序完成的事情，例如权限、时间判断、状态一致性、数据格式校验，不应无必要交给大模型。

---

# 四、用户与组织

## 4.1 组织结构是重要基础，但第一阶段不要设计过度复杂

系统需要有基本的组织概念。

至少希望存在：

- 企业/组织
- 部门
- 用户
- 角色

现阶段角色可以先以：

- 管理者 / Boss
- 普通员工

为主。

同时在设计上给未来的：

- 部门经理
- 部门负责人
- 项目负责人

等角色留下扩展空间。

### 第一阶段的核心不是完整复制真实企业组织架构

不希望因为“要有经理”而提前设计非常复杂的上下级体系、跨部门权限和组织树。

第一阶段更希望：

> **先建立 User + Department + Role 的基础框架。**

例如：

- 技术部
- 产品部
- 运营部

每个员工属于某个部门。

之后如果确实需要经理角色，再进一步扩展。

---

# 五、角色思维

## 5.1 Boss / 管理者

Boss 是会议和企业任务体系中的主要管理角色。

Boss 关注的不是某一条具体任务，而是：

- 企业有哪些会议；
- 最近有哪些重要决定；
- 会议产生了多少任务；
- 任务当前执行到什么程度；
- 哪些任务即将到期；
- 哪些任务已经逾期；
- 哪个部门/人员承担了哪些工作；
- 需要关注哪些异常。

Boss 端希望具备：

- 创建会议
- 设置会议时间
- 设置地点
- 指定参会人
- 设置会议说明
- 查看历史会议
- 查看 AI 分析结果
- 查看任务整体情况
- 查看任务执行状态
- 对 AI 生成的错误任务进行修改、补充或删除
- 后续可以进一步扩展为部门/经理级管理

Boss 不应该成为每一条 AI 结果的人工审核员。

系统的默认方向应是：

> **AI 自动处理，人工进行必要的纠错和兜底。**

---

## 5.2 普通员工

员工端的核心问题不是“我可以管理什么”，而是：

> **我今天要参加什么会？我需要干什么？哪些任务和我有关？现在做到哪了？**

员工工作台希望优先展示：

- 今日会议
- 即将到来的会议
- 我的任务
- 即将到期任务
- 已逾期任务
- 与我有关的协作任务
- 与我有关的历史会议
- AI 工作助手

例如员工登录后，可以很自然地看到：

> 今天 14:00 有产品迭代会议，地点 3 号会议室。
>
> 你当前有 3 个任务，其中 1 个高优先级任务今天到期。
>
> 你参与协作的任务有 2 个。
>
> 昨天会议中新增 1 个与你相关的任务。

系统最终应尽量让员工快速回答：

**“我现在需要做什么？”**

---

# 六、会议管理

## 6.1 会议是谁创建

初步设想：

**主要由 Boss / 管理者创建会议。**

会议属于系统中的核心业务对象。

创建会议时至少涉及：

- 会议名称
- 时间
- 地点
- 创建人
- 参会人员
- 所属部门/业务范围（可根据实际设计）
- 会议说明

---

## 6.2 预约会议

第一阶段应优先支持普通预约会议：

> 2026/09/05 14:00，3 号会议室，产品迭代会议，参会人员若干。

重点是让员工登录系统后能够知道：

- 什么时候开会
- 什么地点
- 是否需要参加

---

## 6.3 周期性会议

例如：

> 每周一 10:00 技术周会

这是一个自然的扩展功能，但不是第一阶段的核心。

如果时间和精力有限：

**先完成普通会议预约，不强求自动生成周期性会议。**

---

## 6.4 会议提醒

希望系统不是“创建会议之后就什么都不管”。

可以存在：

- 会前提醒
- 今日会议提醒
- 即将开始提醒

尤其希望 Boss 在开会前收到：

> **即将开始会议，请确认会议并准备录音。**

这个提醒与后面的 AI 分析链路相连接。

---

# 七、会议录音 / 音视频输入

## 7.1 录音的责任主体

初步设想：

**由 Boss / 会议发起人负责录音。**

系统不需要在第一阶段实现完整的视频会议能力，也不需要自己重新发明 Zoom / 腾讯会议。

更加现实的做法是：

> 会议前提醒 → Boss 负责现实中的会议录音 → 会后把录音/视频提供给系统。

---

## 7.2 支持的输入形式

希望系统不要只依赖一种输入。

理想上支持：

- 音频
- 视频
- 已有会议文字

其中：

**视频可以先提取音频，再走语音识别流程。**

因此：

> 音频 → 转写 → AI 分析

以及：

> 视频 → 提取音频 → 转写 → AI 分析

两条路径在后续可以统一到会议文本。

---

# 八、会议内容分析

这是本项目最核心的 AI 能力之一。

会议经过语音转文字后，不应该只生成一段普通摘要。

希望 AI 尽可能结构化地提取：

## 8.1 会议摘要

回答：

> 这场会议整体讨论了什么？

## 8.2 关键讨论内容

回答：

> 主要讨论了哪些事情？

## 8.3 关键决策

回答：

> 最终决定了什么？

重点区分：

**讨论 ≠ 决策**

## 8.4 重要事项 / 风险 / 问题

例如：

- 某个项目存在延期风险
- 某功能存在技术问题
- 某工作需要进一步确认

## 8.5 涉及部门

例如：

- 技术部
- 产品部

## 8.6 涉及人员

例如：

- 张三
- 李四
- 小红

## 8.7 自动生成任务

例如：

> 张三负责支付模块开发，截止 9 月 6 日，优先级高。

AI 应尽可能把类似自然语言内容转化为结构化任务。

---

# 九、AI 自动生成任务

这是项目最重要的卖点之一。

不希望流程变成：

> AI 先生成任务草稿 → Boss 每一项手动确认 → 然后再入库。

更希望默认方向是：

> **会议结束 → AI 自动分析 → 自动生成合理的任务 → 自动进入任务系统 → 自动关联负责人。**

原因是：

如果每一条任务都需要人工确认，那么 AI 的“自动化价值”会明显下降。

但同时：

**自动生成不等于 AI 拥有无条件写数据库的权限。**

应该形成：

**AI 识别 → Tool/业务接口 → 后端校验 → 数据库写入**

如果出现错误：

- Boss 可以修改
- Boss 可以删除
- 后续可记录 AI 原始结果与人工修正

因此：

> **AI 默认自动执行，人负责纠错和兜底。**

---

# 十、任务是什么

任务不是聊天消息，也不是普通待办事项的简单复制。

任务应具有明确的业务来源。

一个任务希望至少具备：

- 任务标题
- 任务描述
- 来源会议
- 负责人
- 协作人员
- 截止时间
- 优先级
- 状态
- 创建时间
- 完成时间
- 更新记录

其中：

**“来源会议”非常重要。**

因为任务不是凭空出现的。

例如：

> 支付模块重构
>
> 来源：2026/09/01 产品迭代会议

员工应该能够从任务追溯回产生该任务的会议。

---

# 十一、任务优先级

希望 AI 在分析会议时能够识别并给出：

- 高
- 中
- 低

等优先级建议。

例如：

> “这个支付漏洞今天必须修复。”

可以判断为高优先级。

而：

> “有时间再整理文档。”

可以判断为低优先级。

但：

**AI 给出的优先级应该是建议，而不是绝对真理。**

员工或管理者可以修改。

同时应该尽量避免把本来可以用代码明确判断的问题交给 AI。

例如：

> 当前日期已经超过截止日期，而且任务仍未完成。

这是系统逻辑，不需要 AI 决定。

---

# 十二、任务状态

任务不应在完成以后直接消失。

至少希望存在类似：

- 待开始
- 进行中
- 已完成
- 已逾期

员工点击“完成”后：

> 任务状态变为已完成，并保留完成时间和历史记录。

员工首页可以默认隐藏已完成任务，但数据库中不应删除。

因为完成任务本身也是有价值的企业数据。

---

# 十三、任务逾期

逾期首先是系统状态，而不是 AI 判断。

例如：

> 当前时间 > 截止时间  
> 且任务状态 != 已完成

则标记：

> 已逾期

可以在员工端显示：

> ⚠ 已逾期 1 天

管理者可以看到：

> 技术部有 1 项任务逾期。

AI 可以在此基础上给出：

> “该任务已逾期，建议关注。”

但 AI 不负责计算“今天是不是逾期”。

---

# 十四、任务协同到底是什么

本项目中的“协同”不希望做成即时聊天软件。

明确不以：

- 企业微信
- Slack
- 微信聊天
- 在线 IM

为核心。

原因是现实工作中真正的沟通通常发生在线下、电话、企业微信、微信等环境。

系统更适合负责：

> **让参与同一工作的人员知道任务是什么、谁负责、谁协作、当前做到什么程度。**

因此，一个任务可以存在：

- 负责人
- 协作人
- 状态
- 截止时间
- 优先级
- 动态记录

例如：

> 张三：负责后端开发
>
> 李四：负责测试
>
> 张三：进行中
>
> 李四：待开始

这样已经可以形成最基础的任务协同。

---

# 十五、任务动态

为了增强“协同感”，任务可以保存简单的状态动态。

例如：

> 9 月 2 日 14:30，张三将任务状态改为“进行中”。
>
> 9 月 4 日 17:20，张三将任务状态改为“已完成”。

不需要一开始做成聊天消息流。

这里的目标是：

> **让相关人员知道工作已经进行到什么程度。**

---

# 十六、员工之间如何协作

不建议强行设计“催促按钮”。

也不建议一开始做：

- 内部聊天
- @提醒
- 文件协作
- 实时通信
- 在线会议

两个人协作时，系统主要提供：

- 负责人
- 协作人
- 任务状态
- 截止时间
- 任务动态
- 来源会议

现实中的具体工作仍然可以在线下或其他通讯工具完成。

这个设计更符合项目规模，也更符合真实工作方式。

---

# 十七、AI 工作助手

系统应该存在一个 AI 对话入口。

但它不是：

> “你好，我是一个 ChatGPT，有什么可以帮助你？”

这种与业务完全脱离的聊天框。

它应该是：

**企业工作 AI 助手。**

员工可以直接问：

> 我今天有什么任务？

> 我今天有什么会议？

> 昨天会议里有没有提到我？

> 支付模块是谁负责的？

> 上次产品会议决定了什么？

> 我最近有哪些逾期任务？

> 上周关于支付项目讨论过什么？

管理者可以问：

> 本周有多少个任务？

> 哪些任务已经逾期？

> 技术部现在有多少任务在进行？

> 最近几次会议主要都在讨论什么？

---

# 十八、AI 助手的数据来源

AI 助手不应只依赖大模型本身的训练知识。

它主要应理解并使用企业内部数据。

可能包含：

### 结构化业务数据

- 用户
- 部门
- 角色
- 会议
- 参会人
- 任务
- 负责人
- 协作人
- 状态

### 非结构化知识

- 会议转写
- 会议纪要
- 企业文档
- 项目资料
- 制度文件

这样：

> “我还有几个任务？”

适合通过业务数据查询。

而：

> “上个月关于支付项目讨论过什么？”

更适合通过知识检索。

---

# 十九、数据库与知识库的基本思维

本项目预计会同时存在两种性质的数据。

## 19.1 业务数据库

适合保存：

- 用户
- 部门
- 角色
- 会议
- 参会关系
- 任务
- 负责人
- 状态

这些需要确定性、关联性和一致性。

## 19.2 RAG / 向量知识库

适合保存：

- 会议文字片段
- 会议纪要
- 企业文档
- 历史资料

用于：

> “这件事以前有没有讨论过？”

> “之前的会议是怎么决定的？”

等语义检索问题。

**具体使用什么数据库、ORM、向量库、检索策略，后续再决定。**

---

# 二十、权限

权限需要设计，但第一阶段不要把自己做成大型 OA 系统。

基本思路：

## Boss / 管理者

有更高的数据可见性和管理权限。

例如：

- 创建会议
- 管理会议
- 查看较大范围任务
- 调整任务
- 修改 AI 生成结果

## 普通员工

主要查看和处理与自己有关的信息。

例如：

- 查看自己有权限看到的会议
- 查看自己的任务
- 更新自己的任务状态
- 查看协作任务
- 使用 AI 助手

未来如果加入 Manager：

> Boss → Manager → Employee

可以进一步建立部门级管理边界。

但第一阶段不要因为 Manager 的权限问题拖垮整个项目。

---

# 二十一、关于“会议数据是否只属于参与人”

这是一个后续需要讨论的问题。

理想情况下，会议至少要有：

- 创建人
- 参与人
- 可见范围

以后可以进一步支持：

- 全员会议
- 部门会议
- 指定人员会议
- 管理层会议

但第一版只要先建立“会议与用户权限关联”的思想，不需要一次性实现所有复杂规则。

---

# 二十二、Agent 的核心作用

本项目不是“一个页面调用一次大模型”。

Agent 应该能够：

1. 理解用户任务；
2. 判断当前问题需要哪些信息；
3. 检索会议/知识；
4. 调用业务工具；
5. 组织多步流程；
6. 在必要时根据结果选择下一步；
7. 最终给用户可执行、可解释的结果。

例如：

> “找出昨天会议中分配给我的任务，并告诉我哪些已经逾期。”

Agent 可能需要：

**查昨天会议 → 找与当前用户相关内容 → 查任务 → 判断状态 → 汇总回答**

这才是 Agent 的价值。

---

# 二十三、Tool Calling

AI 不应该直接随意操作数据库。

理想思路：

> Agent 决定需要执行什么操作
>
> → 调用一个明确的业务工具
>
> → 后端进行权限检查和参数校验
>
> → 数据库执行
>
> → 返回结果给 Agent
>
> → Agent 继续处理

例如：

- 查询我的任务
- 查询会议
- 创建任务
- 修改任务
- 查询某个员工
- 查询部门任务

这些都可以成为明确的工具。

---

# 二十四、LangGraph / 工作流

系统希望使用工作流来组织复杂的 Agent 行为。

例如：

**问题理解 → 判断类型 → 检索会议 / 检索知识 / 调用工具 → 结果判断 → 必要时 Fallback → 最终回答**

复杂流程可以进行状态保存和条件路由。

重点不是为了“技术栈看起来高级”而使用 LangGraph。

而是因为：

> **真实的 Agent 流程存在多个步骤、状态和分支。**

如果简单的事情不需要 LangGraph，就不强行使用。

---

# 二十五、AI 自我纠错 / 优化

项目对“AI 自动修复”的理解暂时不是：

> AI 修改自己的源代码并重新部署。

这个范围太大。

更合理的方向是：

**AI 结果 → 人工反馈 → 保存修正 → 后续优化**

例如：

AI 认为：

> 负责人 = 张三

Boss 修改成：

> 负责人 = 李四

系统可以记录：

- AI 原始结果
- 最终结果
- 人工修正

以后可以考虑用于：

- Prompt 优化
- 规则优化
- 检索优化
- 结果评估

这形成：

**AI → 执行 → 反馈 → 优化**

的闭环。

---

# 二十六、明确的产品边界

本项目希望做大一些，但不希望无限膨胀。

## 希望做

- 企业基础用户/角色
- 部门基础概念
- 会议创建与管理
- 时间/地点/参会人员
- 会前提醒
- 音频/视频输入
- 语音转文字
- AI 会议分析
- 自动任务生成
- 负责人/协作人员
- 优先级
- 截止时间
- 状态
- 逾期标记
- 员工工作台
- 管理者工作台
- 历史会议
- RAG
- AI 工作助手
- Tool Calling
- Agent 工作流
- 基础权限

## 暂时不做

- 完整即时通讯
- 在线视频会议
- 复杂 OA
- 完整企业 ERP
- 自动加入 Zoom/腾讯会议等第三方会议
- 复杂多级组织架构
- 过度复杂的项目管理
- 大规模多 Agent 系统
- 过度复杂的日历系统

原则是：

> **做一个有完整业务闭环的 AI 企业应用，而不是复制钉钉。**

---

# 二十七、项目真正的核心卖点

如果最终只能说清楚几句话，应当是：

### 卖点 1

**会议结束后，不需要人工重新整理任务。**

AI 自动把会议内容转化成结构化任务。

### 卖点 2

**任务不是孤立的。**

每个任务都可以追溯到产生它的会议。

### 卖点 3

**员工看到的是与自己有关的工作。**

系统根据用户、部门、会议、任务关系组织信息。

### 卖点 4

**AI 不只是总结，还能调用业务工具。**

可以查询会议、查询任务，并在权限允许的情况下执行明确的业务动作。

### 卖点 5

**企业历史信息可以被再次利用。**

过去的会议和企业资料进入知识库后，可以通过 RAG 检索。

### 卖点 6

**形成会议到工作的闭环。**

会议不再是信息终点，而是任务产生和组织协作的起点。

---

# 二十八、项目与课程技术路线的关系

本项目需要尽量吸收课程实践阶段的核心知识，但不应该机械照搬“智能日志分析”的业务场景。

课程后半部分关注的核心能力包括：

- Python 工程化
- 大模型接入
- Structured Output
- Tool Calling
- RAG
- LangGraph
- 状态管理
- 条件路由
- Fallback
- HITL
- Streamlit / Web 产品化
- Prompt 优化
- Token 优化
- PII/数据安全
- 工程质量

本项目希望将这些能力映射到“会议与任务协同”场景。

对应关系大致可以理解为：

**会议文本结构化 → Structured Output**

**企业/历史会议检索 → RAG**

**查询/创建/修改任务 → Tool Calling**

**多步骤 Agent → LangGraph**

**权限与数据隔离 → Session / RBAC 思维**

**检索失败 → Fallback**

**重要操作人工确认 → HITL（后续可扩展）**

**会议数据处理 → Python 工程化**

因此，这个项目本身也是一次：

> **从“能调大模型”向“能设计和构建 AI 应用系统”的实践。**

---

# 二十九、技术选型的基本态度

技术不要为了“看起来高级”而堆。

课程给出的技术路线包括 Python、LangChain、LangGraph、SQLite FTS5、ChromaDB、Regex、Pandas、Streamlit 等。

本项目可以以课程为技术基础，但具体是否继续采用这些技术、是否改成其他组合，应根据：

- 项目复杂度
- 团队熟悉程度
- 15 天时间
- 开发稳定性
- 最终演示效果

来决定。

原则：

> **优先保证系统能跑通业务闭环，再追求技术亮点。**

---

# 三十、Vibe Coding 的开发原则

AI 可以大量参与编码，但不能替代项目设计。

应当保持：

**人负责：**

- 需求
- 产品方向
- 架构决策
- 功能边界
- 业务规则
- 验收标准
- 问题判断

**AI 负责：**

- 探索代码
- 生成实现
- 修改代码
- 编写测试
- 排查问题
- 补充文档

开发过程中建议保持：

**先分析 → 再规划 → 再实施 → 再测试 → 再总结**

而不是：

**想到什么 → 直接生成一大堆代码。**

---

# 三十一、项目文档思维

建议项目根目录逐渐形成：

```text
project/
├── CLAUDE.md
├── PROJECT.md
├── README.md
└── docs/
```

其中：

### PROJECT.md

记录：

- 项目目标
- 产品思维
- 已确定的需求
- 当前状态
- 重要决策
- 暂不做的内容

### CLAUDE.md

记录：

- 给 Coding Agent 的工作规则
- 项目结构说明
- 编码约束
- 常用命令
- 测试方式
- 不应该修改的东西
- 重要开发约定

### README.md

最终用于对外展示：

- 项目介绍
- 功能
- 技术栈
- 架构
- 启动方式
- Demo
- 项目亮点

---

# 三十二、开发时的一个核心原则

**不要一次让 AI 生成整个项目。**

建议：

**先建立项目理解 → 确认整体设计 → 搭基础框架 → 一个模块一个模块开发 → 每完成一块立即测试 → 定期更新项目文档。**

尤其是以下这些决策，应该由人参与：

- 数据模型
- 权限模型
- Agent 工作流
- Tool 边界
- RAG 数据范围
- 自动任务生成规则
- AI 出错时怎么处理

---

# 三十三、目前尚未完全确定的问题

以下问题暂时保留，不强行决定：

1. 最终使用 Vue + Python 哪种具体后端组合；
2. MySQL 还是其他业务数据库；
3. 具体使用哪种 ASR；
4. 具体使用哪种向量数据库；
5. 是否支持真正的实时录音；
6. 是否需要说话人识别；
7. Manager 角色什么时候加入；
8. 会议可见范围的具体权限规则；
9. AI 任务自动入库的具体校验规则；
10. AI 助手是否允许直接执行修改操作；
11. Fallback 具体采用哪种检索策略；
12. 是否加入 HITL 确认机制；
13. 是否使用多 Agent。

这些都应该在后续设计阶段根据实际开发情况决定。

---

# 三十四、项目最终希望给人的感觉

最终希望别人看到这个项目时，不是：

> “这是一个上传会议录音，然后 GPT 总结一下的项目。”

而是：

> **“这是一个把企业会议、人员、任务和内部知识连接起来的 AI Agent 工作系统。”**

它具备：

- 真实的业务场景
- 明确的用户角色
- 真实的数据关系
- AI 内容理解
- RAG
- Agent
- Tool Calling
- 任务自动生成
- 权限
- 任务状态
- 历史追溯
- 一定程度的反馈闭环

最终形成：

**会议管理 → 会议内容 → AI 分析 → 自动任务 → 人员协作 → 执行状态 → 历史知识 → AI 助手**

这条完整链路。

---

# 三十五、当前阶段的最高优先级

在正式写代码以前，最重要的事情不是把每一张数据库表写出来。

当前优先级应该是：

**1. 把产品定位理解清楚。**

**2. 把核心用户流程理解清楚。**

**3. 把 Boss、员工、部门、会议、任务之间的关系想清楚。**

**4. 确定哪些事情由程序负责，哪些事情由 AI 负责。**

**5. 确定什么是项目核心，什么属于后续扩展。**

**6. 再进入数据库、接口、页面和 Agent 工作流设计。**

---

# 三十六、给 AI Coding Agent 的核心上下文

以后让 Claude Code / Codex 开始处理项目时，可以将本文档作为核心背景。

AI 应理解：

> 这是一个面向企业会议场景的 AI Agent 智能分析与任务协同系统。
>
> 它的目标不是做普通会议纪要，也不是做聊天机器人，而是将会议内容转化为可执行、可追踪的组织工作。
>
> Boss 主要负责创建和管理会议；员工主要通过工作台查看会议、任务、协作事项和 AI 信息。
>
> 会议结束后，音频/视频经过转写，AI 自动分析会议内容，识别摘要、决策、人员、部门、任务、负责人、协作人、截止时间和优先级，并尽可能自动进入任务系统。
>
> 任务必须能够追溯到来源会议。员工负责线下/实际完成任务，系统主要记录状态与协作关系，而不是承担即时通讯职能。
>
> AI 助手应理解企业自己的会议、任务和知识，而不是只调用通用大模型知识。
>
> AI 可以通过明确的 Tool 操作业务，但权限、参数校验和数据库一致性必须由业务后端负责。
>
> 系统应该尽可能形成：
>
> **会议 → AI理解 → 自动任务 → 人员执行 → 状态反馈 → 企业知识沉淀**
>
> 的闭环。
>
> 开发时不要为了技术炫技加入不必要的功能，也不要在需求尚未确定时擅自扩大系统边界。
>
> 每次进行重大修改前，应先理解现有架构；复杂功能先分析和规划，再实施；完成后进行测试，并同步重要项目文档。

---

# 三十七、当前结论

目前已经确定的核心方向：

**项目：面向企业会议场景的 AI Agent 智能分析与任务协同系统**

核心产品：

**企业会议 + AI 分析 + 自动任务 + 员工工作台 + 任务协同 + 企业 AI 助手**

核心业务闭环：

**创建会议 → 会议提醒 → 录音/视频 → 转写 → AI 分析 → 自动生成任务 → 自动关联人员 → 员工执行 → 状态更新 → 管理者查看 → 历史会议/知识可检索**

核心理念：

**AI 自动化，而不是 AI 聊天。**

核心边界：

**做一个完整但克制的企业 AI 应用原型，不做钉钉，不做企业微信，不做大型 OA。**

核心开发理念：

**产品思维和工程决策由人掌握，代码实现大量使用 AI 辅助；边学边做，而不是盲目生成。**
