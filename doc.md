# MeetingFlow 开发文档

更新：2026-09-12。供用户与 Codex 逐步实现、验证原型。本文是设计与执行清单，不表示功能已经实现；AI 协作规则见 `codex.md`。

## 1. 目标和交付层次

核心演示：Boss 创建会议并提供文本或录音，系统自动提取摘要、决策和任务；任务进入员工工作台，员工更新状态或协作进展，Boss 查看执行情况。

目标是完整、简洁、能解释的课设原型。按用户独立开发考虑，保留可用于学习和简历的真实实现证据，不追求生产规模。

| 层次 | 内容 | 完成判断 |
|---|---|---|
| A 文本业务闭环 | 登录、人员、会议文本分析、任务自动落库、协作、权限、首页 | 文本输入到员工完成任务及 Boss 查看，全流程可演示 |
| B 首版展示补齐 | 上传录音与转写、查询助手、带来源的会议检索 | 录音到任务及带来源问答已用真实模型验证 |
| C 后续扩展 | 实时录音、语音写操作、更强检索和恢复（经理层已于 2026-09-09 提前实施） | 不作为 A/B 的前置条件 |

不做：即时聊天、在线会议、视频输入、周期会议、外部消息推送、多租户、微服务、复杂审批、复杂组织树、自训练 ASR。首页显示即将开始的会议即可，不引入独立提醒系统。

## 2. 已定决策与实施默认

### 2.1 用户已确认

- Python 后端、本地 MySQL；DeepSeek 官方密钥已提供，转写及 Embedding 采用轻量本地 CPU 模型，后端在 PyCharm 运行。
- 输入支持文本、上传录音；现场录音工具独立处理。
- 自动任务流程优先，减少审核、确认和手动点击，权限必须保持有效。
- 首版原定 Boss、员工；2026-09-09 用户明确要求现在实现经理层。用户说明经理即部门部长，向上作为员工参会、向下管理本部门；Boss 可邀请整个部门，由部长安排具体人员，也可直接点名。
- 权限采用第 4 节规则；首页优先显示角色刚需。

### 2.2 为实施选择的默认方案

本节方案最初作为实施默认提出；2026-09-07 用户已确认基础框架。T01 已安装并验证基础依赖；后续模块引入依赖时继续核实兼容性并记录，不将基础启动视为业务功能通过。

| 部分 | 默认方案 | 边界 |
|---|---|---|
| 后端 | FastAPI + Pydantic + SQLAlchemy + Alembic | 单体应用，HTTP、校验、数据访问、迁移分层 |
| 前端 | Vue 3 + TypeScript + Vite + Element Plus | 少量业务页面，复用组件，不定制复杂设计系统 |
| 数据与附件 | MySQL + 本地附件目录 | 文件保存在 Web 静态目录之外，鉴权后下载 |
| AI 接口 | 分析、转写、问答、Embedding 独立适配器 | 业务代码不绑定提供商；测试替身与真实实现明确区分 |
| 长任务 | 单后端进程内有限并发工作器 + MySQL 作业记录 | HTTP 提交后返回，前端轮询，不引入 Redis/消息队列 |
| 检索 | MySQL 保存片段及向量，本地计算授权片段相似度 | 面向少量演示会议，后续才迁移专用向量索引 |
| 本地启动 | PyCharm 启动后端，另启前端开发服务 | 前端代理后端请求，无需 Docker |

2026-09-07 用户确认采用本节基础框架，并明确必须实际使用 LangGraph。2026-09-08 用户进一步授权“基础用法难度不高就两处都用”，经判断采用基础 StateGraph：T10 会议流程与 T22 只读助手均实际使用节点、固定连线和条件分支。阶段结果仍保存 MySQL，暂不引入额外检查点存储或 LangSmith。工具调用、条件分支和终止控制须有可验证执行记录，不能用接口占位冒充完成。接入时核实并锁定兼容版本。LangChain 不作为独立硬性要求。

T01 已锁定依赖并验证 MySQL 驱动加载；本机 MySQL 客户端版本已核实，服务端版本与实际连接在 T02 核实。模型地址、密钥、名称、ASR 输入限制和 Embedding 能力到接入阶段核实，不猜测用户 API 的能力。

## 3. 核心流程和业务规则

### 3.1 会议到任务

1. Boss 或部门负责人填会议标题、开始时间、参会人；地点和说明可选。创建人从登录态取得。
2. 在会议详情粘贴文本或上传录音。提交一次自动处理，不另设“确认分析”“确认发布”。
3. 音频先转写，文本直接分析；原文及输入版本持久保存。
4. 模型提取摘要、决策、风险、任务候选和来源片段；后端校验结构、人员与日期。
5. 有效候选自动创建任务；异常候选进入“待补充事项”，不阻塞其他有效候选。
6. 任务立即进入负责人、协作人的任务列表；Boss 可修改、补充或删除错误任务。
7. 员工更新状态或填写进展；Boss 看到数据库已保存的执行情况。

每场会议首版只维护一份当前输入，不做多录音合并。重新提供内容创建新输入版本。若已有发布任务，新版本分析仅更新分析结果和候选，不再次自动派发，由 Boss 选择性补建；避免重复任务和覆盖人工修改。首次输入仍全自动。原输入失败重试不属于新版本。

2026-09-11：本地 ASR 开启时上传自动排队，转写保存后进入文本分析流程；支持查看转写、失败重试和使用转写纠错。每次单文件上传，只显示最新附件，旧文件保留私有存储。未配置 ASR 时仅保存待转写附件，不创建空输入。转写期间若有新文本或更新录音则保留转写结果，但不覆盖新输入；重试复用已保存转写。

### 3.2 自动创建与异常规则

| 情况 | 处理 |
|---|---|
| 明确行动事项，能唯一匹配负责人 | 自动创建；缺少描述时使用简洁行动说明 |
| 没有截止时间 | 留空，显示“未指定”，不阻塞创建 |
| 明确日期或可据会议日期解释的相对日期 | 解析后保存；仅日期按上海时区当日 23:59 到期 |
| “尽快”等模糊日期或无法可靠解析 | 截止时间留空，保留原表述，不强猜 |
| 未给优先级 | 默认普通；可由 Boss 修改 |
| 负责人缺失、多人匹配或系统中不存在 | 留存候选、原因和来源；Boss 补充后转任务 |
| 协作人有歧义但负责人明确 | 先建任务，未解析协作人作为待补充信息，不阻塞负责人 |
| 只有讨论、建议或可能性，没有明确执行承诺 | 进入讨论/风险，不自动创建任务 |
| 格式错误或来源校验失败 | 有限自动修复；仍失败则保存异常，不发布未校验结果 |

姓名先结合参会人及部门匹配；未找到时再到指定部门或全体人员中找唯一匹配，不采用激进近音匹配。模型提供语义姓名，后端解析内部用户 ID，不接受模型自行决定身份。

来源摘录必须对应原文片段。原始分析结果供 Boss 纠错，员工任务接口不返回完整分析 JSON。给未参会员工的摘录限定为行动事项相关文本，Boss 可编辑摘录。此规则不能被宣称能自动识别所有敏感内容，更强脱敏另行设计。

### 3.3 任务协同

- 一个负责人、多个可选协作人，首版不为每位协作人拆独立子任务状态。
- 状态：待开始、进行中、已完成。逾期独立计算：未完成且超过截止时间。
- Boss、范围内部门负责人及任务负责人可改状态；完成记录完成时间，重新打开清除当前完成时间但保留事件历史。
- Boss、负责人、协作人可填写简短进展，按时间展示；不做聊天或文件协作。
- Boss 和范围内部门负责人可改任务内容、人员、期限和优先级，可手工补建任务；经理派发负责人限本部门。
- 错误任务软删除，列表、统计、助手均排除；不做回收站页面。
- 普通员工不因同部门就互看任务；部门负责人按第 4 节管理本部门人员负责的任务。

## 4. 权限和部门负责人

登录身份由后端会话确定，不相信请求或模型传入的角色、当前用户 ID。单组织、无开放注册。2026-09-09 用户确认经理就是部门部长，按一个所属部门管理，既向上参会也向下安排工作，不引入多部门管辖、组织树或层级审批。

| 资源/动作 | Boss | 部门负责人（MANAGER） | 员工 |
|---|---|---|---|
| 人员与角色管理 | 全部 | 只读人员目录 | 只读人员目录 |
| 创建会议 | 允许，可按部门邀请或直接点名 | 允许，按部门邀请仅限本部门，可直接点名 | 不允许 |
| 会议全文、纪要与原文 | 全部 | 自建、直接受邀，或本人部门受邀；不因下属单独参会而自动可读 | 直接受邀，或被本部门负责人安排参会 |
| 编辑会议、提交原文、重试及候选处理 | 全部 | 仅自己创建的会议 | 不允许 |
| 安排部门参会人员 | 所有受邀部门 | 仅自己受邀的部门 | 不允许 |
| 原始模型调试结果 | 允许 | 不返回完整调试 JSON | 不允许 |
| 任务列表和详情 | 全部未删除任务 | 本部门人员负责的任务，加本人负责/协作的任务 | 本人负责/协作 |
| 改任务内容、人员、日期；删除 | 允许 | 本部门人员负责的任务，不能向部门外或 Boss 派发负责人 | 不允许 |
| 更新状态 | 允许 | 部门管理范围及本人负责的任务 | 仅本人负责 |
| 添加进展 | 允许 | 当前可见任务 | 本人负责/协作 |
| 首页、助手、检索 | 全部授权数据 | 相同部门/个人任务范围及会议邀请范围 | 个人授权范围 |

任务按负责人的当前部门归属管理，本部门员工仅作为跨部门任务协作人时，部长不自动获得该任务权限；部长本人参与时保留普通个人权限。协作人可以跨部门。Boss 的任务不纳入部长管理，即使 Boss 被设置了部门。账号、调岗、角色设置集中在 Boss 页面，作为简单实现的保守默认，不把“部长也像 Boss”解释成系统管理员权限。

按部门邀请不是向部门全员开放全文：受邀部门的启用负责人自动参会，并选择本部门其他人员；Boss 直接点名与部门安排分开保存，经理清空部门名单不会删掉直接点名。撤回部门邀请删除对应部门安排；直接邀请独立保留。人员调岗后旧部门安排不再授予访问；经理调岗、降级或停用按最新身份失权。部门可暂未设负责人，由 Boss 直接点名或代排；同部门若配置多名负责人，共享该部门管理范围，不增加主副部长机制。

任务来源会议仍独立鉴权：只因部门任务可见，不能读 Boss 或其他人会议全文。会议详情中的任务列表、统计、查询助手、检索复用集中范围；原文中自然可见的工作内容不额外假装隐藏。

后台自动发布在保存任务前复查原文提交人的当前角色、会议管理权限和负责人所属部门；失权或跨部门项留待补充，由 Boss 处理，不以系统工作器身份绕过派发限制。重试不覆盖已发布任务和人工修改。页面操作按钮读取后端能力字段，写请求仍重新鉴权。

## 5. 页面和助手

### 5.1 页面

| 页面 | 首要内容 | 次要操作 |
|---|---|---|
| 登录 | 账号密码 | 标识演示账号，无可绕过鉴权的角色切换参数 |
| Boss 首页 | 近期会议、任务完成/逾期概况、待补充数量 | 创建会议、查看异常 |
| 员工首页 | 今日到期和逾期任务、进行中任务、近期会议 | 开始/完成任务、查看全部任务 |
| 会议列表/详情 | 基本信息、参会人、处理进度、摘要、决策 | 原文折叠、Boss 输入及纠错、授权任务入口 |
| 任务列表/详情 | 我负责/我协作、状态、期限、来源摘录、动态 | 按权限修改或填写进展 |
| 人员管理 | Boss 维护姓名、部门、账号及部门负责人角色 | 不做组织树 |
| AI 助手 | 提问、结果和可点击来源 | 从统一导航进入，不遮挡主流程 |

“今天任务”指今日到期任务，不推测工作日程；无期限任务仍在未完成列表可见。已完成任务可筛选查看。空状态、加载和失败状态说明事实，不用大量图表填充页面。

### 5.2 只读助手

- 2026-09-11：真实 LangGraph 通过 DeepSeek 结构化 JSON 生成受校验的查询计划，执行授权查询并回答；固定按钮、显式关键词和语义检索绕过收费模型。不是提供商原生 function calling 协议，也不接受任意工具名或身份参数。
- 能查任务、会议、逾期与统计，检索历史会议并引用来源；接模型 API 本身不等于能访问这些业务数据。
- 工具限定为 `list_tasks`、`list_meetings`、`task_statistics`、`search_meeting_content`，复用当前会话身份及业务服务。
- 每问最多 1 次只读工具调用，无循环；API 失败明确提示。不提供任意 SQL、任务修改或提权工具。
- 数量和状态由程序计算，会议回答基于实际片段；没有依据时说明没有找到。
- 检索先限制授权会议，再计算片段相关性。未参会任务参与人可通过任务工具读取摘录，不能借会议检索访问全文。
- Embedding 不可用时可用关键词检索验证接口，但必须标识为关键词模式，不称为向量 RAG 验收通过。
- 每次问题独立处理，不做长期记忆。用户问题和会议文本均不能改变系统权限。

## 6. 数据、接口与恢复设计

### 6.1 最小数据对象

| 对象 | 关键内容 |
|---|---|
| Department / User | 姓名、部门、角色、登录名、密码摘要、启用状态、内部 ID |
| Meeting / MeetingParticipant | 标题、时间、地点、说明、创建人、直接点名参会关系 |
| MeetingDepartment / MeetingDepartmentParticipant | 受邀部门及部门安排的参会人，与直接点名独立 |
| MeetingInput | 输入版本、原文或真实转写文本、提交标识、创建时间 |
| MeetingAudio | 录音元数据、私有存储名、转写状态/文本/错误、基准输入、处理人及产出输入关联 |
| ProcessingJob | 输入版本、阶段、状态、次数、错误摘要、起止时间 |
| Analysis / ActionCandidate | 原始输出、校验结果、候选序号、来源、待补充原因、对应任务 |
| Task / TaskCollaborator | 内容、负责人、协作人、期限、优先级、状态、完成时间、会议/候选来源及摘录、软删除 |
| TaskEvent | 操作人、时间、类型、必要变更及进展正文 |
| MeetingChunk | 输入版本、片段序号、原文及偏移、512 维向量与模型/窗口策略标识 |
| Session | 随机令牌摘要、用户、过期时间 |

使用迁移管理表结构。数据库 ID 不需要用户输入或记忆。UTC 保存时间，上海时区解释和显示。人员停用而非硬删除，历史任务保持关联；首版不删除已有任务的会议，不允许停用最后一个 Boss。

2026-09-07 实施收敛：按用户“先简单实现、目录规范”的要求，T02 先落地人员、会议、任务、协作、动态与会话共 8 张业务表；输入、作业、分析候选和检索表在 T05/T08/T10/T21 随功能增加独立迁移。上表是完整首版目标，不代表 T02 已一次性建立所有表。当前数据关系与代码索引见 `docs/project-guide.md`。

### 6.2 API 边界

统一 `/api` 前缀；字段在对应任务中落为 Pydantic 模型及 OpenAPI，不另维护手写的重复接口定义。

- `/auth/login`、`/auth/logout`、`/auth/me`：登录、退出、当前用户；HttpOnly 会话 Cookie，写请求验证可信来源，部署 HTTPS 时启用 Secure。
- `/departments`、`/users`：目录与 Boss 管理接口，普通员工响应不含登录名、密码摘要等内部字段。
- `/meetings`、`/meetings/{id}`：创建、列表、详情和 Boss 更新。
- `/meetings/{id}/inputs`：文本提交后自动排队，202 返回 job ID；一次提交携带请求标识，网络重试复用。
- `/meetings/{id}/audio`：POST 原始文件字节，查询参数 filename 与 UUID request_id；返回 201 和附件元数据。逐段限制实际大小，默认 50 MB，基础文件头校验 mp3/wav/m4a。
- `/meetings/{id}/audio/{audio_id}/download`：按会议全文权限下载；没有静态文件地址。`/transcribe` 为开始或重试入口，复用已排队、运行或成功结果；未配置 ASR 时返回 503。
- `/meetings/{id}/inputs/{input_id}/chunks/{chunk_id}`：按当前会议权限读取指定历史版本引用；检索只返回最新版本，已引用的旧版本仍能明确标识查看。
- `/jobs/{id}`、`/jobs/{id}/retry`：按会议权限查看进度；重试由 Boss 或该会议创建经理执行。
- `/meetings/{id}/analysis`、`/meetings/{id}/candidates`：分析展示及 Boss 待补充处理；员工响应与内部原始结果隔离。
- `/tasks`、`/tasks/{id}`、`/tasks/{id}/status`、`/tasks/{id}/events`：查询、Boss 管理、状态和进展。
- `/dashboard`：当前身份的首页数据，统计范围与列表一致。
- `/assistant/query`：授权工具/检索后返回回答及来源，无写工具。

列表默认每页 20、最多 100。未登录 401，禁止操作 403，不可见详情 404，冲突 409，输入错误 422。错误返回可读消息，服务日志保存诊断但不记录密钥和完整会议正文。

### 6.3 阶段恢复和幂等

实际流程：录音队列转写 → 保存文本及分片 → LangGraph 分析和发布；文本输入跳过转写。向量在查询时按需生成，不另设 INDEX 后台作业。转写和分析分别保存排队、运行、成功、失败状态；索引失败不撤销已生成任务，查询明确回退关键词模式。

2026-09-10 实施简化：当前无外部依赖的文本分片在保存 MeetingInput 的同一事务完成，不增加 INDEX 后台作业；分析失败的原文也可检索。每片最多 600 字，重叠 100 字，优先换行边界；保存精确字符偏移，先按 SQL 权限与最新版本过滤，再做字面关键词匹配，最多 5 片。历史输入通过 `python -m scripts.index_meetings` 一次补齐，可重复运行。2026-09-11 本地向量按需生成并缓存：600 字来源片段以 200 字窗口、160 字步长计算后归一化平均，避免模型 token 上限遗漏尾部；保存 bge-small-zh-v1.5:mean200:v1 标识，标识不匹配时重算。最多扫描最近 1000 个授权当前版本分片，阈值 0.45，返回至多 5 片。

- 事务保存输入和作业；同请求标识只创建一次，同会议运行中拒绝第二个分析提交。
- 工作器用条件更新认领作业，启动后将上次中断的运行作业重新排队。首版只启动一个应用进程，不宣称多实例恢复能力。
- 阶段先持久化结果再推进；已有转写不重复转写，已有校验分析不重复调用模型。
- 真实 DeepSeek HTTP 不自动重试；非法 JSON 或顶层结构错误允许最多 1 次修复调用；初次和修复各自限时 60 秒，失败保存错误，人工可重试失败阶段。测试适配器保留最多 3 次尝试用于恢复验证；真实模式仅一次外层尝试，控制费用。
- 任务与分析候选建立唯一关联；发布任务、协作人、创建事件与候选状态在同一事务提交。重复发布返回已建任务。
- 待补充项复用同一发布服务；已发布任务补协作人只更新关联，不新建任务。重试不能覆盖人工修改。
- 新输入版本遵守第 3.1 节规则，不与原输入失败重试混淆。
- 模型调用设置有限超时、并发上限，具体值按提供商限制配置；前端约每 2 秒轮询，结束或离开页面后停止。

## 7. Codex 分模块任务清单

状态：`[ ]` 未开始，`[~]` 进行中，`[x]` 已验收，`[!]` 外部阻塞。实现与验收均完成才能勾选。测试替身不能代替真实 AI 验收。

从 T01 按依赖推进，一次完成一个任务或紧密关联的小组。2026-09-07 用户已授权逐步开发；沿用已确认设计，新的关键设计点通过问答确认后实现，不一次性生成全部系统。

### 模块一：基础与权限

- [x] **T01 环境与最小骨架**｜依赖：无。检查 Python、Node、MySQL 版本；建立前后端目录、健康接口、配置示例、依赖锁定和忽略规则。验收：两端启动、健康接口正常，配置无真实密钥。无需模型 API。
- [x] **T02 数据模型与演示数据**｜依赖：T01。建立基础业务迁移与初始化脚本，两个部门、Boss、员工，覆盖未参会协作人；AI 表随后续模块迁移。验收：空库迁移成功，初始化重复运行不重复建账号，密码保存摘要。
- [x] **T03 登录及目录接口**｜依赖：T02。会话登录/退出/当前用户、部门人员接口和 Boss 维护接口。验收：过期、退出、账号停用使会话失效；员工不能改角色；目录字段无越界；最后一个 Boss 不能停用。
- [x] **T04 集中权限策略**｜依赖：T03。动作判断、会议/任务查询范围及经理扩展入口。验收：Boss、负责人、协作人、参会者、无关员工、未知角色矩阵通过，猜 ID 无法越权。当前验证集中查询与详情取数函数；业务 HTTP 端点在 T05/T06 实现时另行验收。

### 模块二：会议与任务业务

- [x] **T05 会议接口**｜依赖：T04。创建、更新、查询、参会人和文本输入版本保存。验收：员工只能读参与会议，不能创建或提交输入。
- [x] **T06 任务与协作接口**｜依赖：T04、T05。Boss 手工建任务、分配、状态、进展、事件、逾期及软删除。验收：协作人不能改状态；负责人可以；完成/重开、无期限、删除过滤正确。
- [x] **T07 首页数据**｜依赖：T05、T06。聚合角色所需会议、任务、统计。验收：统计与当前身份列表一致，同部门无关任务不计入员工首页。

### 模块三：文本自动分析

- [x] **T08 分析适配器与输出契约**｜依赖：T05。定义结构化输出、候选与来源；测试适配器及真实接口插槽。验收：正常、缺字段、无任务、错误结构均有样例，测试模式明确标识。
- [x] **T09 人员、日期、来源解析**｜依赖：T08、T02。实现第 3.2 节规则。验收：同名不同部门、缺负责人、相对日期、缺期限、无原文依据均有明确结果。
- [x] **T10 LangGraph 作业及阶段恢复**｜依赖：T08。基础 StateGraph、持久作业、工作器、有限重试、阶段保存、进度和重启恢复；MySQL 阶段结果作为恢复依据。验收：真实图执行、分析失败复用原文、重启恢复中断作业、错误不泄露密钥。
- [x] **T11 自动发布与待补充**｜依赖：T06、T09、T10。有效候选自动落库，异常独立留档，补充后发布。验收：同候选重复执行只建一个任务，异常不阻塞有效项，重试不覆盖人工改动。
- [x] **T12 输入触发完整流程**｜依赖：T11。自动排队、分析、发布，处理重复提交和新版本。验收：首次输入无需逐项确认，网络重试无重复任务，重新分析不再次自动派发。

### 模块四：可展示页面

- [x] **T13 登录、导航、首页**｜依赖：T03、T07。两类首页及空状态。验收：两类账号看到对应数据，退出后无法加载受保护资源。
- [x] **T14 会议与处理页面**｜依赖：T12、T13。创建、列表、详情、文本提交、进度、摘要、折叠原文、Boss 异常补充。验收：提交自动显示处理结果，失败可重试，员工无管理能力。
- [x] **T15 任务协作页面**｜依赖：T06、T13。负责/协作筛选、详情、状态、进展、Boss 修改、来源入口。验收：未参会参与人仅见摘录，不能看全文；无关同事无法访问；保存后数据刷新。
- [x] **T16 文本闭环验收**｜依赖：T14、T15。测试适配器跑 Boss 输入→任务→员工反馈→Boss 查看，覆盖异常候选。验收：无逐项审核，权限和重复请求检查通过；记录是否接入真实模型。

### 模块五：真实模型和录音

- [x] **T17** 真实分析接入｜DeepSeek 官方 deepseek-flash，结构化输出、超时及认证/余额错误处理已实现；真实短样例生成可校验结果，密钥不进入日志。
- [x] **T18** 音频与 ASR｜mp3/wav/m4a 默认 50 MB、600 秒，本地 Whisper base CPU/int8；私有存储、鉴权下载、转写队列恢复、旧输入保护及真实短语音到分析已验证。
- [x] **T19** 音频页面与重试｜上传、转写进度、失败重试、查看转写与文本纠错已实现；转写保存复用，真实录音链路到员工完成任务通过，样例来源及错误如实记录。

### 模块六：助手与历史知识

- [x] **T20 授权查询工具**｜依赖：T07；真实模型调用端另依赖 T17。任务、会议、统计工具复用业务与权限。验收：数量来自数据库，工具参数不能变更当前身份或扩大范围。
- [x] **T21** 分片和检索｜持久化版本分片、关键词及本地 BGE 语义检索、来源跳转已验证；向量按需保存并记录模型标识，权限和最新版本先经 SQL 过滤。窗口、阈值及数量限制见 6.3。
- [x] **T22** LangGraph 只读助手与页面｜真实模型 JSON 查询计划 → 至多一次授权工具 → 回答/帮助；结构校验拒绝身份参数，无写工具。自由查询、来源、失败、权限及无结果分支已验证，保留免费固定入口。

### 模块七：收尾和学习证据

- [x] **T23 基础人员管理页面**｜依赖：T03、T13。Boss 维护姓名、部门、员工账号与启用状态；部门负责人配置由 T28 补充。验收：员工不能调用管理接口，停用人员的历史仍可追溯，最后一个 Boss 受保护。
- [x] **T24** 综合回归与运行说明｜168 项后端测试、前端构建、真实浏览器链路及独立临时空库迁移初始化/登录/任务闭环通过；README 已更新安装、配置、启动、试用与限制。
- [x] **T25** 结果与后续清单｜docs/model-validation.md 记录一个合成短语音样例、实际错字、纠错入口、3 次收费请求、检索与效果限制，不虚构准确率或规模。

### 模块八：经理层（2026-09-09 用户新增授权）

- [x] **T26 经理权限设计与基础整合**｜核对原预留，确认管辖部门、跨部门任务、会议创建和人员管理边界；由后端返回详情操作能力，避免页面自己推断资源权限。已按用户部长/部门邀请说明落地第 4 节，复杂权限不增加。
- [x] **T27 经理后端与自动发布**｜依赖 T26。角色与迁移、人员配置、集中 SQL 范围、任务管理、会议/输入/候选权限、后台发布时复查权限；调岗/降级后权限按最新数据生效。禁止只改前端或只改手工入口。
- [x] **T28 经理页面与演示验收**｜依赖 T27。部门概览、任务操作、会议能力、Boss 设置经理；新增独立演示账号和跨部门用例，不修改原五个账号的演示角色。验证列表、详情、统计、助手、检索、重试和自动派发范围一致，更新试用说明。

## 8. 验收与进度更新约定

必须检查：

- 文本和真实录音自动生成任务，缺期限不阻塞，缺负责人留待补充。
- 补充、重复提交、重复执行和重启不重复建任务；人工修改不被恢复覆盖。
- Boss、负责人、协作人、参会者、未参会受派人、无关同事的权限符合矩阵。
- 列表、详情、统计、附件、助手、检索和来源链接全部执行权限，参与关系变化后新请求生效。
- 模型超时/错误、转写失败、索引失败有明确阶段状态，重试复用成功阶段。
- 已完成任务留存历史，逾期、无期限和空数据展示正确。
- 助手无依据不编造；恶意会议文本不能诱导越权或写操作。

后端使用 pytest：纯规则单元测试、MySQL 事务/查询集成测试及 HTTP 权限测试。唯一约束、事务和持久化恢复不能仅用内存替身验收。前端进行类型检查、构建和角色流程验证，不为低影响布局编写镜像测试。

每次任务结束记录：任务 ID、变更、执行检查与结果、外部阻塞/限制、下一任务。未执行检查写“未运行”，代码写完但未验收不可标已完成。实际日志随开发追加到本节，避免不断复制旧对话。

### 2026-09-07 · T01 已验收

- 变更：建立 `backend/`、`frontend/`；加入 FastAPI `/api/health`、独立环境配置、PyCharm 启动入口、Vue 连接检查页、前端开发代理、配置示例、忽略规则及 README 启动说明。页面只是基础连通性检查，不代表业务页面设计已定。
- 环境：项目 `.venv` 使用 Python 3.12.10；Node 24.8.0、npm 11.6.0；MySQL 客户端 8.0.45、MySQL80 服务运行。原 PyCharm 仍指向 Miniconda Python 3.14.7，README 已说明应选择项目 `.venv`，没有改动用户 IDE 配置。
- 依赖：后端完整版本锁定到 `backend/requirements.txt`，主要为 FastAPI 0.141.1、Pydantic 2.13.5、SQLAlchemy 2.0.52、Alembic 1.19.2、PyMySQL 1.2.0；前端直接版本及完整依赖树已锁定，主要为 Vue 3.5.42、Element Plus 2.14.5、Vite 8.2.2、TypeScript 5.9.3。LangGraph 在 T22 接入时安装与锁定。
- 已执行并通过：后端依赖一致性检查；ORM、迁移工具、MySQL 驱动导入及引擎构造（未连接数据库）；配置敏感字段隐藏显示、跨工作目录加载与上海时区可用检查；前端类型检查与生产构建；后端实际启动后健康接口及 OpenAPI 检查；前端实际启动后 HTML、Vue 模块转换及 `/api/health` 代理请求均为 HTTP 200，代理返回预期健康 JSON。
- 限制：数据库连接和服务端版本未验证；未接入登录、业务 API、模型或 ASR；未执行浏览器交互/视觉验收及业务测试。本页只有连通性检查，不将这些未实现项标为已通过。健康接口只表示进程存活。
- 环境问题处理：npm 默认缓存超出可写目录，已改为项目 `work/npm-cache`；Windows 受限执行仍阻止前端依赖/临时目录写入，经过工具审批后安装、构建和启动成功，无需更改全局环境。
- 下一任务：T02 数据模型、迁移与演示数据。已询问使用新建 `meetingflow` 库还是现有项目专用库，等待用户选择；实际连接信息到本地配置时获取，不记录密码到开发文档。

### 2026-09-07 · T02 已验收

- 变更：新建本地 `meetingflow` 数据库；基础迁移 `0001` 建立 8 张业务表（另有 Alembic 版本表）；模型按人员、会议、任务、会话分文件；提供数据库配置脚本与演示初始化脚本。
- 真实验证：MySQL 服务端 8.0.45；空库 `alembic upgrade head` 成功，`alembic check` 无模型差异；13 项密码与 MySQL 测试通过，覆盖重复初始化及人工改名保留、用户名唯一、成员关系唯一、外键、合法角色、任务完成时间约束、UTC 往返和事务回滚。
- 初始化：2 个部门、5 个账号、1 场会议、1 项任务；覆盖 Boss、负责人、仅参会者、未参会协作人及无关员工。再次运行新增均为 0，原密码与已有内容保留。密码使用随机盐 scrypt 摘要，随机演示密码只写本地 `backend/.env.demo`，数据库连接仅写本地 `backend/.env`。
- 环境盘点：i5-13500HX（14 核/20 线程）、约 15.7 GB 内存、D 盘约 156 GB 可用；Conda 26.7.1、Git 2.55.0、Node/npm 和 MySQL 已有。现有环境足够当前外部 API 原型，无需本地大模型或 GPU 环境。保留已验证 `.venv`；提供 `environment.yml` 供 Conda 重建，未新建或声称验证了 Conda 项目环境。
- 实际问题：驱动将 MySQL CHECK 违例作为 OperationalError 返回，测试已按数据库错误码 3819 判断；普通工具权限下 `.env` 临时文件创建发生持续重试，经工具审批完成写入，无需修改用户全局配置。
- 边界：数据库测试使用真实 MySQL 事务并回滚，当前还未实现 HTTP 权限；演示记录明确为脚本生成，不是 AI 结果；初始化脚本供单人本地串行执行。下一任务 T03。

### 2026-09-07 · T03/T04 已验收

- T03：实现 `/auth/login`、`/auth/logout`、`/auth/me`、部门列表/创建/更名及人员列表/创建/更新。随机 Cookie 会话只存摘要，默认 8 小时，HttpOnly + SameSite=Lax；写请求校验 Origin/Referer，响应不缓存；校验错误不回显密码输入。员工目录只返回 ID、姓名、部门，管理员字段与密码摘要隔离。停用/改角色撤销会话，最后一个启用 Boss 受保护；锁定启用 Boss 记录后判断，避免简单的先查数量后修改。
- T04：`app/permissions.py` 集中角色检查、会议/任务 SQL 可见范围、授权详情取数、状态修改与进展权限。经理/未知角色默认拒绝；参会不等于能看任务；未参会协作人可看任务但不能读会议；移除关系后新查询失权；软删除任务对所有角色隐藏。
- 验证：全部 38 项测试通过；覆盖 HTTP 登录、退出、过期、停用及重新启用不恢复旧会话、再次登录令牌轮换、伪造角色、员工管理越权、人员字段隔离、最后一个 Boss 停用/降级保护及完整角色关系矩阵；`alembic check` 再次通过。通过真实前端代理完成 Boss/员工登录→当前用户→目录→退出→401，演示会话均已退出。
- 已知限制：2 项测试依赖弃用提示（Starlette 的 httpx 与 AnyIO 接口），不影响本次测试，不进行无关升级；未做并发负载验收。会议/任务/附件/助手 HTTP 入口尚未实现，后续必须复用集中权限并继续进行端点验收，不能由 T04 推断全部产品接口已安全完成。
- 本阶段不增加开放注册、密码找回、自助改密码、复杂权限框架。页面仍为 T01 检查页，登录和业务页面在 T13 后实现。下一任务 T05 会议接口。

### 2026-09-07 · T05 已验收

- 变更：会议创建/更新/列表/详情，参会人维护、当前原文与按输入 ID 读取历史版本；迁移 `0002` 增加 `meeting_inputs`。列表分页统计先按登录身份过滤，不返回其他会议原文。
- 文本保存：原文不裁剪，最多 100000 字符，用 MySQL MEDIUMTEXT 支持长中文；UUID 提交标识与版本号分别唯一。同标识同内容复用已有记录，不同内容返回 409；新标识增加版本。锁定会议行后分配版本。
- 验证：新增 9 项真实 MySQL/HTTP 测试通过，覆盖创建者从会话确定、重复参会人去重、时区、停用人员拒绝、员工读写权限、移除参会人立即失权、版本及重试、跨会议猜输入 ID、分页、8 万中文字符保存。迁移已在本地执行。
- 阶段边界：提交目前返回 201（重试 200）、`processing_status=SAVED`，只表示原文已保存，没有作业或 AI 结果。T12 接入自动处理后改为既定 202 作业流程；前端尚未提供此操作，不额外增加“确认分析”产品步骤。
- 额度约束：用户要求接近 5 小时额度上限自动停；按约 85% 留余量收尾。本阶段结束账户读数为 5 小时已用 13%、周已用 22%。下一任务 T06。

### 2026-09-07 · T06 已验收

- 变更：任务创建/详情/列表/更新/软删除，状态修改、进展和动态分页；支持我负责/我协作、状态、逾期及来源会议过滤。会议内任务列表独立过滤任务权限。响应仅带会议标题和摘录，不带全文；返回是否能打开来源会议的提示，但授权仍由后端执行。
- 规则：Boss 管任务信息和人员，负责人可改状态，协作人仅写进展；完成保存时间，重开清空当前完成时间但保留动态。同一状态重试不重复生成事件。仅日期解释为上海当日 23:59；无期限不算逾期。关联变更及事件同事务保存，删除后任务详情、动态和列表都不可见。来源摘录修正只记录修改事实，不把旧摘录重新暴露给任务参与人。
- 验证：新增 12 项真实 MySQL/HTTP 测试通过，覆盖五类角色读写、未参会协作人、仅参会人员、完成与重开、重复状态、重新分配即时失权、软删除、各种筛选、空/错误参数、停用人员和日期。现有集中权限已复用于所有新增入口。
- 额度：本阶段结束 5 小时已用 23%、周已用 24%。下一任务 T07；已提前询问会议分析是否也采用 LangGraph，等待用户设计选择。

### 2026-09-08 · T07 已验收

- 变更：`GET /dashboard` 统一当前身份的任务统计、今日到期/逾期/进行中任务卡片及即将开始的会议，每类卡片最多 5 项；统计不被卡片上限截断。所有查询复用 T04 范围，并以同一次查询的当前时间计算。
- 验证：新增 8 项真实 MySQL/HTTP 测试通过，五类账号首页统计与任务列表一致；上海午夜及下一日边界、已完成/删除/无期限任务、会议权限和空首页均通过。
- 边界：待补充候选尚未实现，数量返回 null 表示未接入，不伪报 0；首页页面在 T13 实现。5 小时额度本阶段后已用 32%，周已用 25%。下一任务 T08/T09。

### 2026-09-08 · T08 已验收

- 变更：`app/ai/` 中建立模型无关协议、结构化候选模型、提示词、测试适配器和分析入口；真实提供商未配置时明确失败。提示词和原文分消息，模型只提供语义姓名/部门和原始期限，不接受模型给用户 ID 或权限。
- 行为：结构错误最多修复 1 次，整体调用有超时和输出大小上限；单个候选格式错误单独留档，不丢弃其他有效项。提供商异常只返回固定错误码，测试结果明确标记 fixture。
- 验证：新增 8 项测试通过；正常、缺负责人、无任务、错误结构均有 JSON 样例；包含部分候选错误、有限修复、超时、未配置和原文消息隔离检查。未经真实 API 验收，不声称防注入或模型提取准确率已经验证。
- 下一任务：T09 确定性解析，再用 LangGraph 编排阶段执行。

### 2026-09-08 · T09 已验收

- 变更：按启用人员、姓名、指定部门与参会优先级进行匹配；保守解析明确日期、今天/明天/后天、若干天后、本周/下周以及明确时分；来源逐字匹配并保存字符起止位置。只有明确承诺、来源有效、负责人唯一的候选标为 READY，缺负责人为 NEEDS_INFO，讨论单独标记，来源/结构错误拒绝发布。
- 验证：新增 19 项检查通过，含真实数据库目录读取；T08/T09 合计 27 项检查通过。覆盖同名跨部门、参会优先、未知/停用人员、跨年、相对日期、非法日期、模糊期限、缺期限、协作人歧义不阻塞负责人、无原文依据和讨论候选。
- 限制：日期解析只覆盖明确列出的表达，未支持的自然语言保留原表述、期限留空；不做近音名匹配。模型对执行承诺和人员语义的判断仍需真实样例验收，后端规则不能保证模型绝不误提取。
- 下一任务：T10 基础 LangGraph 分析作业，发布与输入自动触发随后由 T11/T12 接入。

### 2026-09-08 · T10 存档暂停（未标为已验收）

- 用户要求准备休息，在最近的合适存档点暂停；本次不再新增模块，不安排自动唤醒，等待下次明确继续。
- 已实现：安装并锁定 LangGraph 1.2.11；迁移 `0003` 增加 `processing_jobs` 和 `analyses`；真实 StateGraph 执行 LOAD → ANALYZE → VALIDATE → PERSIST → COMPLETE_ANALYSIS，已有分析结果走复用分支。MySQL 保存结果、节点轨迹与作业状态，暂时性错误最多自动重试 2 次，提供作业查询/失败重试和按会议权限读取当前分析结果。
- 验证：T10 新增 7 项检查通过，使用真实 MySQL 和测试模型适配器；覆盖实际图执行、结果持久化、临时错误有限重试、错误脱敏、模拟中断重排队、保存结果后重试不再调模型、原始输出仅 Boss 可见、同会议活动作业互斥与新输入不显示旧分析。`pip check` 通过；`alembic check` 无差异。此前 T01—T07 全量 67 项通过，T08/T09 分批 27 项通过；安装 LangGraph 后尚未重新跑完整测试集，不能称当前完整回归已通过。
- 当前边界：作业 scope=ANALYSIS_ONLY，成功仅表示分析已完成，不表示已发布任务；T11 发布、T12 输入自动排队尚未实现。后端生命周期的工作器入口已写，`analysis_worker_enabled` 默认 false。T10 仍为进行中，下一次先核验实际生命周期启动/取消/重启以及数据库故障时行为，再确认验收，不为了凑完成度展开 T11。
- 依赖说明：LangGraph 带入 langchain-core、LangSmith 等传递依赖，但未配置外部追踪服务；图执行显式关闭追踪。新增 httpx2 传递依赖后，最新 T10 测试只剩 1 项 AnyIO 弃用提示；旧 2 项提示属于历史记录。
- 运行状态：存档检查未发现 8000、5173 端口监听，先前后端 PID 33660 已不存在；本轮没有启动常驻分析工作器，没有待处理数据库作业（processing_jobs 为 0 条）。依赖安装及迁移均已结束。未重启前后端，避免睡前新增后台工作。
- 额度：收尾前账户实时读数为 5 小时已用 70%、周已用 31%，低于约 85% 的收尾阈值。本次因用户要求休息而提前暂停，不是额度耗尽。

### 2026-09-08 · T10 接续：故障恢复已补，等待本机数据库启动

- 用户明确继续，本次初始账户额度 5 小时/周均为 0%。代码沿用昨日存档，无数据库重建、密码调整或依赖升级。
- 修复：原工作器在启动恢复、领取作业或保存阶段遇到数据库异常后可能直接退出；现在保留作业恢复依据，按 1、2、4…最多 30 秒退避重连，连接恢复后重新排队中断作业。日志只记录异常类型；关闭时可打断等待，取消中的分析由下次启动恢复。
- 新增 6 项生命周期/故障检查，覆盖应用实际 lifespan 的启动、取消与重启，恢复/领取/完成阶段数据库异常，以及关闭配置。应用生命周期和事务边界核对了 [FastAPI 官方说明](https://fastapi.tiangolo.com/advanced/events/) 与 [SQLAlchemy 官方说明](https://docs.sqlalchemy.org/en/20/orm/session_transaction.html)。
- 已执行：不依赖数据库的筛选集合 27 项通过；随后离线关闭与禁用配置 2 项通过（其中禁用配置与前批有重叠，不相加宣称全量）。当前仍有 1 项 AnyIO 弃用提示。
- 尚未验收：MySQL80 当前停止，实际连接被拒绝；启动服务遭遇 Windows 管理员权限不足，已请用户在 Windows 服务管理中启动。第一次实库测试在准备数据时失败，不能视为功能断言失败或验收通过；剩余 4 项新增实库检查、原 7 项流程检查及完整回归待数据库恢复后运行。T10 保持进行中，T11 尚未开始。
- 本次未启动前后端常驻服务；已终止本次失败测试残留的 PID 23440/27460，没有遗留测试任务。收尾额度 5 小时已用 16%、周已用 3%。

### 2026-09-08 · T10 已验收

- 用户启动 MySQL80 后确认服务 Running、启动类型 Automatic，迁移为 0003。此前停止原因未调查，不修改系统自启设置。
- 真实 MySQL 下原流程与新增生命周期合计 13 项通过；新增依赖后的完整后端回归 107 项通过，Alembic check 无差异。应用启动、取消、重启和数据库故障退避恢复已验收；错误输出不包含数据库或提供商秘密。
- 工作器仍按单应用进程设计，真实模型尚未配置。接续 T11 自动发布与待补充，再进入 T12 自动触发。

### 2026-09-08 · T11 已验收

- 迁移 0004 增加候选事项表及任务的唯一候选关联。自动发布与 Boss 补充共用任务插入服务；任务、协作人、创建事件和候选状态同事务提交。
- 有效项自动发布，缺负责人/结构/来源异常独立保留；讨论不派发。发布前复查启用人员，负责人失效仅阻止该项，协作人失效保留待补充。Boss 可补全后生成、忽略未发布项，或给已建任务补协作人。
- 同候选重复发布返回已有任务，修改、移除协作人、软删除均不会被重试覆盖或重建。待补充首页数量仅 Boss 可见，统计当前输入且排除已删除任务的遗留提醒。
- 新增 11 项真实 MySQL/HTTP 检查通过，连同任务与首页回归合计 31 项通过；数据库唯一约束、发布中断回滚、部分异常不阻塞、五类账号权限、补充重试及停用人员均覆盖。Alembic check 无差异。
- 下一步 T12 将输入提交和作业保存合成事务，把发布节点接入 LangGraph；前端尚未完成，真实模型尚未接入。

### 2026-09-08 · T12 已验收

- 迁移 0005 为作业增加 scope，保留旧分析作业 ANALYSIS_ONLY；正常文本入口建立 FULL_PIPELINE。输入版本和作业同事务保存，提交及网络重试均返回 202、job_id 和实际处理状态。同会议活动作业期间拒绝新输入，相同请求标识仍可复用。
- LangGraph 实际执行 LOAD → ANALYZE → VALIDATE → PERSIST → PUBLISH → COMPLETE；重启或失败重试复用已保存分析、候选和任务。阶段名实时持久保存，默认随单进程后端启动一个工作器。
- 已有任务（含人工创建或后来删除）时，新版本候选进入 REVIEW，由 Boss 选择性补建；没有已发布任务的新版仍允许自动发布。旧版候选不允许新增派发，旧任务保持可用且人工修改不被覆盖。
- 新增 6 项端到端检查，连同输入、图和生命周期合计 28 项通过；T01—T12 完整后端回归 124 项通过，Alembic check 无差异。输入与作业原子回滚、重复提交、未配置模型显式失败、发布后中断重试、新版规则及未参会任务参与人权限均覆盖。
- 真实模型尚未接入；默认适配器返回 ANALYSIS_NOT_CONFIGURED，不用假结果冒充真实 AI。分析功能目前通过明确标识 fixture 的样例验证。

### 2026-09-08 · T13 已验收并存档

- 用户确认浅色界面、左侧导航、清晰列表，首页突出待办/逾期/处理异常。实现真实账号登录、会话恢复、退出、Boss/员工首页，统一请求错误处理及会话失效清理。统计与卡片直接读取后端，无演示硬编码数字。
- 首阶段导航链接到首页各分区；会议列表/编辑/提交和任务操作页面继续由 T14/T15 增加，不宣称已有完整业务页面。桌面左侧导航，窄屏改为顶部紧凑导航。空状态、加载、刷新、服务故障均有展示。
- 依赖锁定 Vue Router 5.3.1；核实了[官方安装说明](https://router.vuejs.org/installation.html)及[导航守卫](https://router.vuejs.org/guide/advanced/navigation-guards.html)。开发验证使用 playwright-core 1.63.0 和本机已有 Chrome，按[官方浏览器支持说明](https://playwright.dev/docs/browsers#google-chrome--microsoft-edge)复用浏览器，未下载另一份浏览器。
- 验证：前端类型检查与生产构建通过；真实浏览器检查错误密码、Boss/员工统计对照真实接口、导航、刷新恢复、退出后后退、真实会话撤销、窄屏无横向溢出。额外模拟 503 检查错误与重试恢复。无浏览器运行异常，4 张截图在 work/screenshots；已目视检查登录、Boss 首页和手机首页。
- 本轮在 T13 验收点保存，不新增 T14。下一步会议列表/详情、创建与文本处理页面，然后任务协作页面；暂不扩展模型提供商或助手设计。

### 2026-09-08 · T14/T15/T16 文本演示闭环已验收

- 用户补充先完成第一版后集中测试修改；暂时没有模型 API，授权先做可演示版本。界面和权限继续沿用已确认设计。
- 完成会议搜索/分页、创建/编辑、原文提交与版本、每 2 秒进度轮询、失败重试、纪要、折叠原文、异常补充/忽略和任务入口。重复提交保留请求标识，离开页面停止轮询与请求；失去权限时清空原文和结果。
- 完成任务分页及负责/协作/状态/逾期/来源会议筛选、详情、状态变化、进展、动态、Boss 手工创建/修改/软删除；相关来源仅在后端允许时提供会议链接。修改任务其他字段不丢失原截止时间中的时分。
- 增加严格固定样例的 DemoAnalysisAdapter，默认 demo，结果明确标记 demo。普通文本返回 DEMO_SAMPLE_REQUIRED，不冒充真实 AI；样例实际经过 LangGraph、确定性校验、事务发布和缺负责人补充。配置接口仅向 Boss 提供样例，密钥/密码不入接口。
- 前端类型和生产构建通过；新增演示适配器/流程 9 项相关后端测试通过。真实 Chrome 验证创建、样例处理、补充、协作反馈、负责人完成、Boss 修改、无关账号拒绝和移动端布局通过。完整后端回归待本轮剩余模块结束后统一执行。
- 保留演示会议“演示验收：接口与测试安排”（ID 271）、任务 ID 233 等 2 项：一项已完成并有协作动态，一项补充后待执行。脚本 frontend/scripts/business-smoke.mjs 可复验，明确会保留这场演示数据，不清用户数据。截图位于 work/screenshots。
- 下一步人员管理与演示查询助手。T17 真实模型、T18/T19 实际录音链路因外部 API 未提供，不能标为真实能力验收。

### 2026-09-08 · 演示查询与人员管理已验收

- 按用户“没有接口，先可演示”的授权，T20 授权工具先独立于真实模型落地；任务、逾期、近期会议、统计调用现有权限服务。T21 先提供明确标注 keyword 的当前原文搜索，SQL 在读取正文前过滤会议权限与旧版本；尚无持久化分片、向量或 Embedding 验收，保持进行中。
- T22 的演示分支实际使用第二个 LangGraph：固定问法分类 → 单次授权查询/直接帮助 → 回答。最多 1 次工具调用，比首版上限 3 更严格；没有模型决策、自由问答或写工具，不宣称真实模型助手完成。调用轨迹和模式可在接口查看，页面不显示内部实现细节。T22 保持进行中，后续真实适配器接入后再做完整验收。
- 新增 7 项真实 MySQL/HTTP 检查通过，覆盖五类账号结果一致性、动态失权、旧原文不命中、恶意原文不扩权、写请求只返回说明、用户身份字段不能注入、实际图执行及调用次数。
- T23 页面完成账号创建/编辑/启停、角色和部门归属、部门创建/改名；员工导航不显示管理入口且路由拦截，后端继续执行全部权限。浏览器验证创建并停用 demo_validation、最后 Boss 不能停用、员工无管理入口通过；保留的校验账号已停用。
- 查询助手浏览器验证真实会议关键词来源、员工无权检索会议但可查自己的协作任务、写请求无来源返回通过。前端类型及构建通过；截图 people.png、assistant.png 保存于 work/screenshots。
- 下一步仅做本轮综合回归、演示说明和边界记录。用户无真实 API，T17/T18/T19 及 T21/T22 真实模型部分不伪报完成。

### 2026-09-08 · 文本演示版综合验收与存档

- 后端完整回归 134 项通过；Alembic check 无差异、pip check 无依赖冲突。首次回归发现旧空首页测试假设李四没有任务，与新保留的演示任务冲突；改为事务内独立空账号后完整复验通过，未删除演示或用户数据。剩 1 项 AnyIO 弃用提示。
- 前端类型检查与生产构建通过；真实 Chrome 的登录/首页/会话、业务闭环、人员/助手三组检查通过。截图已保存并目视检查桌面及移动布局；测试使用独立浏览器会话，结束退出。
- 新增 docs/demo-guide.md，写明五个账号角色、样例处理至反馈完成的步骤、可复验命令与实际数据影响；README 和 codex.md 同步现状。没有把密码写入文档。
- 当前为 1 份固定会议文本的可演示流程，覆盖有效承诺、缺负责人和讨论 3 类事项；实际保留 2 项任务，其中 1 项人工补充负责人，1 项有反馈及完成记录。这不是模型准确率评估，没有真实提取/识别/性能数据。
- T24/T25 的演示部分完成，原计划真实模型、录音、语义检索及全新空库重演仍未验收。用户参与确认了技术栈、两处 LangGraph、界面方向、权限主线和无接口时先演示；没有代替用户确认新的外部服务。
- 收尾前 5 小时额度已用 84%、周 27%，停止新增模块，保存于可试用节点。下一轮优先处理用户集中试用反馈；真实接口可用后再接入和评估。

### 2026-09-09 · T26/T27 部门负责人及部门邀请已验收

- 用户确认经理即部门部长，简单按部门分；Boss 可邀请部门，由部长安排人员，也可直接点名。角色、部门范围和会议邀请规则已重写到第 4 节；账号/角色管理仍集中 Boss 作为实施默认。
- 迁移 0006：支持 MANAGER 且必须有所属部门，增加 meeting_departments 与 meeting_department_participants 两表，直接点名继续用原参会表，保留已有数据。现共 14 张业务表；Alembic check 无差异。
- 集中权限覆盖部门任务、个人参与任务、自己主持的会议、受邀部门会议、候选、重试、统计、助手及检索。部门邀请不等于全员可读；自动发布复查提交人当前角色与派发范围。调岗撤销相关用户会话，旧部门参会安排动态失效。
- 既有 134 项后端回归通过；新增经理 11 项实库检查通过，覆盖角色/部门约束、部门任务、个人协作、向上参会不可管理、部门安排和直接点名独立、撤回/调岗失权、统计/助手范围、后台发布失权及跨部门阻止。前端类型与构建通过，真实页面闭环仍在 T28 验证。
- 用户恢复 MySQL 后迁移和实库检查成功；没有修改自启或数据库密码。新增 tech_manager（陈部长，技术部）、product_manager（周部长，产品部）演示账号，初始化只新增不覆盖原人员，密码沿用对应演示员工，未写入文档。

### 2026-09-09 · T28 经理页面与最终回归已验收

- 真实 Chrome 验证 Boss 按部门邀请并直接点名、部长安排本部门人员、员工受邀可读、其他部门被拒、部长不能编辑 Boss 会议、部长主持样例会议自动派发并完成任务。新增菜单/角色、负责人筛选、手机布局通过；保留会议 456/457 和任务 384。
- 最终完整后端回归 145 项通过（含 11 项经理检查）；前端类型/构建、经理及原有三组浏览器检查通过。新增检查进一步覆盖候选补充不得越部门、其他经理猜候选 ID 被拒、经理不读取原始模型调试输出。
- 初次整体验证发现测试对全库待补充为零、部门只有一位经理的假设，与保留演示数据冲突；改为比较现有基线与本次变化后完整通过，未清库。保留 1 项 AnyIO 弃用提示。
- README、试用指南、工程索引和 codex.md 已同步。收尾额度 93%，在 T28 验收点存档，不启动下一个大模块；真实模型接口仍最后接入。

### 2026-09-10 · T21 分片检索、T18/T19 录音基础验收

- 用户再次授权按课设尺度继续完成；Git 与真实 API 以后提供，不引入额外服务或子代理。
- 迁移 0007/0008 已应用：新增 meeting_chunks 与 meeting_audio，当前 16 张业务表及 Alembic 版本表，Alembic check 无差异。为已有输入回填 2 个片段，不修改原文、任务或已有人员。
- 文本保存时同事务创建最多 600 字/重叠 100 字的分片；带输入版本和精确原文位置。检索先 SQL 授权和过滤最新输入，最多返回 5 片；引用链接可查看明确标识的历史原文，撤回参会权限立即失效。没有向量或语义检索。
- 录音按原始字节流上传，默认上限 50 MB；实际字节计数、基础文件头、空文件及路径文件名校验。随机文件名保存在 backend/storage/audio，鉴权下载；UUID 提交标识和内容摘要防止重复上传。详情展示最新附件，已有文本与任务保持有效。
- 当前统一为待转写；预留转写入口明确返回未配置 503，不假造文本、处理进度或发布任务。真实 ASR 限制、可解码性和识别质量仍待真实服务验证。
- 完整后端回归 160 项通过（原 145 + 新增 15），前端类型和构建通过；新增 Chrome 验证上传失败/成功、下载、原文保留、来源跳转、员工权限与手机布局，桌面/手机截图已目视检查。经理检索测试另补正向可检索基线后单项通过。
- 新增/复用演示会议 787“演示验收：录音保存与分片检索”，录音 29 为明确标注的静音校验 WAV，不是真实会议录音或 ASR 评估。保留该会议的样例任务与待补充事项，原演示数据未清理。
- 测试使用真实 MySQL 事务回滚。原缓存写权限和收尾停顿通过禁用 pytest cacheprovider 避开，最终仅剩已有 AnyIO 弃用提示；旧配置接口测试已适配新增公开能力字段。本轮不重复旧的四组浏览器流程，既有验收记录仍为 2026-09-09。

### 2026-09-11 · T17—T19、T21/T22、T24/T25 完成

2026-09-11：课设首版 A/B 主线已完成，T01—T28 按本文课设范围验收。真实 DeepSeek 分析及只读问答、本地 CPU Whisper base 转写、BGE-small-zh-v1.5 语义检索均已接通。迁移 0009，16 张业务表及版本表；后端 168 项通过，前端类型/构建、真实浏览器闭环、独立空库迁移初始化及任务完成验证通过。

本次共 3 次付费请求（输入 1188、输出 182 token），后续不为重复验证继续调用。会议 891 和任务 668 保留实际结果；语音为本机合成短样例，出现“字段”“测试用例”识别错误，可在页面纠正转写。完整证据及边界见 docs/model-validation.md，不宣称真实会议准确率或生产性能。

真实链路使用合成语音，未完成广泛真实会议效果评估；各项勾选代表课设首版范围，非生产性能认证。新增测试包含模型错误无重试、身份参数拒绝、检索权限及版本隔离、转写幂等和过期输入保护。README、试用指南和项目指南已同步。

### 2026-09-12 · 评估后的最小修复

2026-09-12 小修完成：按用户“课设够用、小范围修 bug、不重复全面测试”的要求，仅明确缺负责人事项/协作安排的提取规则，以及修复非法 JSON 未进入结构修复、两次调用共用超时预算的问题。初次分析与最多一次修复各有 60 秒上限，实际等待两次合计约不超过 120 秒（另加本地处理），HTTP 仍不自动重试，助手不增加修复调用。36 项相关离线测试通过，零新增收费请求；没有重新跑完整回归或真实模型，原 168 项为此前完整验收记录。提示词改进不等于真实效果已重新验证。页面、数据库结构、权限、ASR 和检索保持原方案。

## 9. 后续演进方向

- **更复杂的组织关系**：当前部门负责人已实现；只有用户明确需要时再增加多部门管辖、部门层级或主副负责人。
- **更多自动操作**：只读助手稳定后增加任务修改工具，继续使用后端授权；确认需求按具体操作决定，模型不能自行提权。
- **更强恢复**：实际出现并发和多进程需求后替换作业执行器，考虑队列或 LangGraph 检查点，保留发布幂等约束。
- **更好检索**：有评估样例后再考虑向量库、混合检索、重排，保持权限过滤和原文版本关系。
- **更多输入**：实时录音工具导入、说话人识别、多文件合并及视频分别排期。
- **生产能力**：部署、备份、审计、敏感信息治理、组织隔离、成本管理另定目标，不夹带到课设首版。

## 10. 当前进度

2026-09-13：答辩材料已交付于 docs/defense，采用用户确认的深色、蓝色点缀、大字风格，8 页约 5 分钟。离线 HTML 配 PDF 备用稿和逐页讲稿，展示产品主线、真实截图、LangGraph/权限/检索及验证边界。个人身份暂未填写，演示无需联网或调用模型；末页本机系统链接需另行启动和登录。当前未制作 PPTX，也未修改应用功能。

2026-09-12 小修完成：按用户“课设够用、小范围修 bug、不重复全面测试”的要求，仅明确缺负责人事项/协作安排的提取规则，以及修复非法 JSON 未进入结构修复、两次调用共用超时预算的问题。初次分析与最多一次修复各有 60 秒上限，实际等待两次合计约不超过 120 秒（另加本地处理），HTTP 仍不自动重试，助手不增加修复调用。36 项相关离线测试通过，零新增收费请求；没有重新跑完整回归或真实模型，原 168 项为此前完整验收记录。提示词改进不等于真实效果已重新验证。页面、数据库结构、权限、ASR 和检索保持原方案。

答辩操作及常见现象解释见 docs/demo-guide.md；其他 AI 的原评估及本轮处置见 question.md。

2026-09-11：课设首版 A/B 主线已完成，T01—T28 按本文课设范围验收。真实 DeepSeek 分析及只读问答、本地 CPU Whisper base 转写、BGE-small-zh-v1.5 语义检索均已接通。迁移 0009，16 张业务表及版本表；后端 168 项通过，前端类型/构建、真实浏览器闭环、独立空库迁移初始化及任务完成验证通过。

本次共 3 次付费请求（输入 1188、输出 182 token），后续不为重复验证继续调用。会议 891 和任务 668 保留实际结果；语音为本机合成短样例，出现“字段”“测试用例”识别错误，可在页面纠正转写。完整证据及边界见 docs/model-validation.md，不宣称真实会议准确率或生产性能。

本机 .env 已启用 deepseek/local/local，密钥只存该本地忽略文件，不再询问用户提供。两个模型已下载到 backend/storage/models，运行时使用 CPU，本机 RTX 4060 Laptop 8GB 未启用 GPU 推理。测试 fixture 强制 demo/unconfigured/keyword，避免收费；旧演示浏览器脚本已加模式保护。

入口 http://127.0.0.1:5173/；MySQL80 已启动。账号密码仅在 backend/.env.demo。前后端日志 work/backend-0911.*.log、work/frontend-0911.*.log，操作进程前核验 PID。README 已说明 PyCharm 运行 backend/run.py、前端单独启动、迁移、模型开关和排错。旧会议 271、456、457、787 保留；787 的静音附件仅用于上传测试。

下一步是用户试用、短真实录音验证和反馈修正，按需要少量使用收费接口；没有待提供的 ASR/Embedding 密钥或必须新增的 skill/MCP。2026-09-11 用户已授权建立 GitHub 仓库并推送首版，覆盖此前延后 Git 的约定；仓库为 https://github.com/yhyym7/MeetingFlow，私有、main 分支。注册按 Boss 创建账号的既定方案，没有开放注册、密码找回或自助改密码；复杂组织、实时录音、部署等不扩入当前课设。
